plugins {
	id 'java'
	id 'org.springframework.boot' version '3.5.6'
	id 'io.spring.dependency-management' version '1.1.7'
}


group = 'de.htw'
version = '0.0.1-SNAPSHOT'
description = 'Web Tech Note App'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(21)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-web'
	developmentOnly 'org.springframework.boot:spring-boot-devtools'
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    implementation 'org.postgresql:postgresql'
    // This adds the H2 in-memory database for local development/testing
    runtimeOnly 'com.h2database:h2'
    // PDF generation: Flying Saucer renders XHTML+CSS → PDF via OpenPDF; Jsoup normalizes HTML to XHTML
    implementation 'org.xhtmlrenderer:flying-saucer-pdf-openpdf:9.4.0'
    implementation 'org.jsoup:jsoup:1.22.1'
}

tasks.named('test') {
	useJUnitPlatform()
}
